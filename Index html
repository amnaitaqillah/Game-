      <!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Game Tembak-Tembakan Palestina</title>

  <!-- ====== CSS LANGSUNG DI SINI ====== -->
  <style>
    :root {
      --hitam: #000;
      --putih: #fff;
      --merah: #d32f2f;
      --hijau: #388e3c;
    }

    body {
      margin: 0;
      font-family: "Poppins", sans-serif;
      background-color: var(--hitam);
      color: var(--putih);
      overflow-x: hidden;
    }

    /* ===== HEADER ===== */
    header {
      background: linear-gradient(to right, var(--hijau), var(--hitam), var(--merah));
      text-align: center;
      padding: 20px;
      animation: slideDown 1s ease-in-out;
    }

    header h1 {
      margin: 0;
      font-size: 2em;
    }

    @keyframes slideDown {
      from { transform: translateY(-50px); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }

    /* ===== NAVIGASI ===== */
    nav {
      display: flex;
      justify-content: center;
      gap: 20px;
      background: var(--hitam);
      border-top: 2px solid var(--hijau);
      border-bottom: 2px solid var(--merah);
      padding: 10px;
    }

    nav a {
      color: var(--putih);
      text-decoration: none;
      font-weight: bold;
      transition: all 0.3s;
    }

    nav a:hover {
      color: var(--merah);
      transform: scale(1.1);
    }

    /* ===== HALAMAN UTAMA ===== */
    .hero {
      background: url('https://i.ibb.co/XChBscY/battle-bg.jpg') center/cover no-repeat;
      text-align: center;
      padding: 100px 20px;
      color: var(--putih);
      animation: fadeIn 2s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    .hero h1 {
      font-size: 3em;
      color: var(--hijau);
      text-shadow    
